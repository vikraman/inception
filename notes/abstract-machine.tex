\documentclass[acmsmall,nonacm,natbib=false]{acmart}
\settopmatter{printacmref=false, printccs=false, printfolios=false}

\setcopyright{none}

\usepackage{vc-math}
\usepackage{vc-macros}
\usepackage{parskip}
\usepackage{enumitem}

\newcommand{\APP}{\textsf{app}}
\newcommand{\PMC}{\ensuremath{\textsf{pm}^{\textsf{c}}}}
\newcommand{\PMV}{\ensuremath{\textsf{pm}^{\textsf{v}}}}
\newcommand{\RET}{\textsf{return}}
\newcommand{\PUSH}{\textsf{push}}
\newcommand{\VAR}{\textsf{var}}
\newcommand{\SUB}{\textsf{sub}}
\newcommand{\VTS}{\ensuremath{\vdash^{\textsf{v}}}}
\newcommand{\CTS}{\ensuremath{\vdash^{\textsf{c}}}}
\newcommand{\ID}{\textsf{id}}
\newcommand{\STR}{\textsf{str}}
\newcommand{\EVAL}{\textsf{ev}}
\newcommand{\LABEL}{\textbf{V}}
\newcommand{\unit}{\textbf{1}}

\newcommand{\tail}{\textsf{tail}}
\newcommand{\flag}[1]{{}^{\bullet}{#1}}

%\usepackage{showframe}

\begin{document}
\title{Abstract Machine}
\author{Anonymous}

%\maketitle
\section{Abstract Machine}
\pagestyle{empty}
\thispagestyle{empty}

\subsection{Terms}
We have value terms and computation terms (subject to term formation rules, which are provided elsewhere).
For the purposes of the abstract machine, we distinguish between full terms and partial terms.
Every full term is also a partial term.

Full value terms are:

\quad
$x$,
$\ast$,
$\lambda x . M$,
$(L , R)$,
$\PMV ( M , (x,y).N )$

Full computation terms are:

\quad
$\RET (M)$,
$\PUSH (M , x.N)$,
$\APP (M , N)$,
$\VAR (M)$,
$\SUB (a.M, N)$,
$\PMC ( M , (x,y).N )$

Further partial value terms are:

\quad
$(\_ , R)$,
$(L , \_)$,
$\flag{(L , R)}$,
$\PMV(\_, (x , y).N)$

Further partial computation terms are:

\quad
$\APP(\_ , N)$,
$\PMC(\_, (x , y).N)$

Typing rules for values:

\begin{align*}
   \frac{x : X \in \Gamma}{\Gamma \VTS x : X}
&& \frac{\Gamma , x : X \CTS M : Y}{\Gamma \VTS \lambda x . M : X \Rightarrow Y}
&& \frac{\Gamma \VTS M : X \quad \Gamma \VTS N : Y}{\Gamma \VTS (M , N) : X \times Y}
\end{align*}
\begin{align*}
\frac{\Gamma \VTS M : X \times Y \quad \Gamma, x : X , y : Y \VTS N : Z}{\Gamma \VTS \PMV(M, (x , y).N) : Z}
&& \frac{}{\Gamma \VTS \ast : \unit}
\end{align*}

Typing rules for computations:

\begin{align*}
   \frac{\Gamma \VTS M : X}{\Gamma \CTS \RET (M) : X}
&& \frac{\Gamma \CTS M : X \quad \Gamma, x : X \CTS N : Y}{\Gamma \CTS \PUSH (M , x.N) : Y}
&& \frac{\Gamma \VTS M : X \Rightarrow Y \quad \Gamma \VTS N : X}{\Gamma \CTS \APP(M, N) : Y}
\end{align*}
\begin{align*}
   \frac{\Gamma \VTS M : \LABEL}{\Gamma \CTS \VAR_X (M) : X}
&& \frac{\Gamma , a : \LABEL \CTS M : X \quad \Gamma \CTS N : X}{\Gamma \CTS \SUB (a.M , N) : X}
&& \frac{\Gamma \VTS M : X \times Y \quad \Gamma, x : X , y : Y \CTS N : Z}{\Gamma \CTS \PMC(M, (x,y).N) : Z}
\end{align*}

\clearpage
\subsection{Interpretation}
Types $A$ are interpreted as sets $\llbracket A \rrbracket$ and contexts $\Gamma = x_1 : A_1 , \dots, x_n : A_n$ as products of sets $\llbracket \Gamma \rrbracket = \llbracket A_1 \rrbracket \times \dots \times \llbracket A_n \rrbracket$ as usual.

Value terms $\Gamma \VTS M : A$ are interpreted as morphisms $\llbracket \Gamma \rrbracket \xrightarrow{\llbracket M \rrbracket} \llbracket A\rrbracket$.
\begin{enumerate}
\item $\llbracket x_1 : A_1 , \dots, x_n : A_n \VTS x_i : A_i \rrbracket = \pi_i$
\item $\llbracket \Gamma \VTS \lambda x.M : A \Rightarrow B \rrbracket = \lambda \llbracket M \rrbracket$
\item $\llbracket \Gamma \VTS (M, N) : A \times B \rrbracket = \langle \llbracket M \rrbracket, \llbracket N \rrbracket \rangle$
\item $\llbracket \Gamma \VTS \PMV(M , (x , y).N) : C \rrbracket = \llbracket N \rrbracket \circ \alpha \circ \langle \ID , \llbracket M \rrbracket \rangle$
\item $\llbracket \Gamma \VTS \ast : \unit \rrbracket = \langle \rangle$
\end{enumerate}

Computation terms $\Gamma \CTS M : A$ are interpreted as morphisms $\llbracket \Gamma \rrbracket \xrightarrow{\llbracket M \rrbracket} K_{\LABEL}\llbracket A\rrbracket$.
\begin{enumerate}
%\item $\llbracket \Gamma \CTS \RET (M) : A \rrbracket = \eta \circ \llbracket M \rrbracket$
\item $\llbracket \Gamma \CTS \RET (M) : A \rrbracket \;\gamma\; k = k \;(\llbracket M \rrbracket\; \gamma)$
\item $\llbracket \Gamma \CTS \PUSH (M, x.N) : B\rrbracket \;\gamma \; k = ({\llbracket N \rrbracket}^{\sharp} \circ \STR \circ \langle \gamma , \llbracket M \rrbracket \;\gamma \rangle) \; k$ \newline \hspace*{\fill} where ${\_}^{\sharp} : (X \to K_\LABEL Y) \to (K_\LABEL X \to K_\LABEL Y)$
\item $\llbracket \Gamma \CTS \APP(M , N) : B \rrbracket \; \gamma \; k = (\EVAL \circ \langle \llbracket M \rrbracket , \llbracket N \rrbracket \rangle) \; \gamma \; k$
\item $\llbracket \Gamma \CTS \VAR(L) : \LABEL \rrbracket \; \gamma \; k = \llbracket L \rrbracket \; \gamma$
\item $\llbracket \Gamma \CTS \SUB(a.M , N) : A \rrbracket = \llbracket M \rrbracket \; (\gamma , \llbracket N \rrbracket \, \gamma \, k) \; k$
\end{enumerate}

\clearpage
\subsection{States}

States are pairs $(t ; k)$, where $t$ is a stack of partial terms in an environment (a pair of a partial term and an environment) and $k$ is a stack of continuations.
If the partial term at the top of the stack $t$ is a partial value term, then $(t ; k)$ is a \emph{value state}, otherwise $(t ; k)$ is a \emph{computation state}.

An \emph{environment} is a map from label variables to computation states and from non-label variables to full terms in an environment.
\vspace{10pt}

\subsection{Transitions}
\begin{align*}
	%
	%\intertext{Variable lookup:}
	%((x, \gamma), \tail ; k) &\leadsto \left\{
	%\begin{array}{ll}
	%	((\pi_1 \gamma (x) , \pi_2 \gamma (x)) , \tail ; \pi_3 \gamma (x)) & \text{if $\gamma (x)$ is a c.s.} \\
	%	((\pi_1 \gamma (x) , \pi_2 \gamma (x)) , \tail ; k) & \text{otherwise}
	%\end{array}
	%\right.\\
  %
	\intertext{From value states to value states:}
	((x, \gamma), \tail ; k) &\leadsto ((\pi_1 \gamma (x) , \pi_2 \gamma (x)) , \tail ; k)\\
	\intertext{\hspace*{\fill}where $x$ is a non-label variable}
	\\
  (( (L , R) , \gamma ) , \tail ; k ) &\leadsto (( L , \gamma ) , ( (\_ , R) , \gamma) , \tail ; k)\\
  (( \flag{(L, R)}, \gamma), (\PMV ( \_ , (x , y).N) , \gamma') , \tail ; k) &\leadsto (( N , \gamma' [ x \mapsto (L , \gamma) , y \mapsto (R , \gamma) ]) , \tail ; k )\\
  (( \PMV (M , (x,y).N), \gamma) , \tail ; k) &\leadsto (( M , \gamma) , (\PMV (\_ , (x,y).N) , \gamma) , \tail ; k )\\
	\\
	%
  (( T , \gamma) , ( (\_ , R) , \gamma') , \tail ; k ) &\leadsto (( R , \gamma' ) , ((T , \_) , \gamma') , \tail ; k )\\
  (( T , \gamma) , ( (L , \_) , \gamma') , \tail ; k ) &\leadsto ((\flag{(L , T)} , \gamma') , \tail ; k )\\
  %(( T , \gamma ) , (\RET (\_) , \gamma') , \tail ; k ) &\leadsto (( \flag{\RET (T)} , \gamma') , \tail ; k)\\
	\intertext{\hspace*{\fill}where $T = \lambda x . M$ or $T = \flag{(L , R)}$ or $T = \ast$}
  %
	% computations
	%
	\\
	\intertext{From value states to computation states:}
  (( \lambda x . M , \gamma) , ( \APP(\_ , N) , \gamma') ; k ) &\leadsto (( M , \gamma [ x \mapsto N ]) ; k)\\
	\\
  (( \ell , \gamma) ; k ) &\leadsto ((\pi_1 \gamma (\ell) , \pi_2 \gamma (\ell)) ; \pi_3 \gamma (\ell))\\
	\intertext{\hspace*{\fill}where $\ell$ is a label variable}
	\\
	\intertext{From computation states to value states:}
  (( \APP (M , N) , \gamma ) ; k ) &\leadsto (( M , \gamma) , (\APP (\_ , N) , \gamma) ; k )\\
  (( \VAR (M) , \gamma) ; k ) &\leadsto ((M , \gamma) ; k)\\
	\\
	\intertext{From computation states to computation states:}
  %(( \RET (M) , \gamma) , \tail ; k ) &\leadsto ( (M , \gamma) , (\RET (\_) , \gamma) , \tail ; k)\\
  %(( \flag{\RET (\_)}, \gamma) , \tail ; x.N , k) &\leadsto (( N , \gamma[ x \mapsto M]) , \tail ; k)\\
  (( \RET (M) , \gamma) ; x.N , k ) &\leadsto ( (N , \gamma[ x \mapsto (M , \gamma , k)]) ; k)\\
  (( \PUSH (M , x.N) , \gamma) ; k ) &\leadsto (( M , \gamma ) ; x.N , k )\\
  (( \SUB (a.M , N ), \gamma) ; k ) &\leadsto (( M , \gamma [a \mapsto (N , \gamma, k)] ) ; k )
\end{align*}

\clearpage
\subsection{Examples}

%Example:
%
%Let $X = \lambda x . x$, $Y = \lambda y . y$ and ${\color{blue}\gamma_0 = (x \mapsto (X , \emptyset) , y \mapsto (Y , \emptyset))}$.
%Then:
%
%\begin{alignat*}{2}
%           &  \big(\big( \PMV ((a,b) = \PMV ((c,d) = (x,y), (d,c)), \PMV ((e, f) = (b, a), e)),  \gamma_0 \big); {\color{blue} k_0} \big)\\
%  \leadsto &  \big(\big( \PMV ((c , d) = (x , y) , (d , c))) , \gamma_0 \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( (x , y) , \gamma_0 \big) , \big( \PMV ((c , d) = \_ , (d , c))) , \gamma_0 \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( x , \gamma_0 \big), \big( (\_ , y) , \gamma_0 \big) , \big( \PMV ((c , d) = \_ , (d , c))) , \gamma_0 \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( X , \emptyset \big), \big( (\_ , y) , \gamma_0 \big) , \big( \PMV ((c , d) = \_ , (d , c))) , \gamma_0 \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( y , \gamma_0 \big) , \big( (X , \_) , \gamma_0 \big) , \big( \PMV ((c , d) = \_ , (d , c))) , \gamma_0 \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( Y , \emptyset \big) , \big( (X , \_) , \gamma_0 \big) , \big( \PMV ((c , d) = \_ , (d , c))) , \gamma_0 \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( \flag{(X , Y)} , \gamma_0 \big) , \big( \PMV ((c , d) = \_ , (d , c))) , \gamma_0 \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( (d , c) , {\color{blue}\gamma_1 = \gamma_0 [c \mapsto X , d \mapsto Y]} \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( d , \gamma_1 \big) , \big( (\_ , c) , \gamma_1 \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( Y , \gamma_1 \big) , \big( (\_ , c) , \gamma_1 \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( c , \gamma_1 \big) , \big( (Y , \_) , \gamma_1 \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( X , \gamma_1 \big) , \big( (Y , \_) , \gamma_1 \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( \flag{(Y , X)} , \gamma_1 \big) , \big( \PMV ((a,b) = \_ , \PMV ((e , f) = (b , a) , e) ), \gamma_0 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( \PMV ((e , f) = (b , a) , e) , {\color{blue}\gamma_2 = \gamma_0[a \mapsto Y , b \mapsto X]} \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( (b , a) , \gamma_2 \big) , \big( \PMV ((e , f) = \_ , e) , \gamma_2 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( b , \gamma_2 \big), \big( (\_ , a) , \gamma_2 \big) , \big( \PMV ((e , f) = \_ , e) , \gamma_2 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( X , \gamma_2 \big), \big( (\_ , a) , \gamma_2 \big) , \big( \PMV ((e , f) = \_ , e) , \gamma_2 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( a , \gamma_2 \big), \big( (X , \_) , \gamma_2 \big) , \big( \PMV ((e , f) = \_ , e) , \gamma_2 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( Y , \gamma_2 \big), \big( (X , \_) , \gamma_2 \big) , \big( \PMV ((e , f) = \_ , e) , \gamma_2 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( \flag{(X , Y)} , \gamma_2 \big) , \big( \PMV ((e , f) = \_ , e) , \gamma_2 \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( e , {\color{blue} \gamma_3 = \gamma_2[e \mapsto X, f \mapsto Y]} \big) ; k_0 \big)\\
%  \leadsto &  \big(\big( X , \gamma_3 \big) ; k_0 \big)\\
%\end{alignat*}

Let $M = \RET (\lambda x . x)$.
Then:
%
\begin{align*}
           &  \big( \big( \SUB(e_1. \PUSH (\SUB(e_2 . \RET (e_2) , \RET (e_1)) , \ell.\VAR (\ell) ) , M) ,  {\color{blue}\gamma_0 = \emptyset} \big) ; {\color{blue} k_0} \big)\\
  \leadsto &  \big( \big( \PUSH (\SUB(e_2 . \RET (e_2) , \RET (e_1)) , \ell.\VAR (\ell) ) ,  {\color{blue}\gamma_1 = \gamma_0[e_1 \mapsto ((M , \gamma_0) ; k_0)]} \big) ; k_0 \big)\\
  \leadsto &  \big( \big( \SUB(e_2 . \RET (e_2) , \RET (e_1)) ,  \gamma_1 \big) ; {\color{blue} k_1 = \ell.\VAR (\ell) , k_0} \big)\\
  \leadsto &  \big( \big( \RET (e_2) , {\color{blue}\gamma_2 = \gamma_1[e_2 \mapsto ((\RET (e_1) , \gamma_1) ; k_1)]} \big) ; k_1 \big)\\
  \leadsto &  \big( \big( \VAR (\ell) , {\color{blue}\gamma_3 = \gamma_2[\ell \mapsto ((e_2 , \gamma_2); k_0)]} \big) ; k_0 \big)\\
  \leadsto &  \big( \big( \ell , \gamma_3 \big) ; k_0 \big)\\
  \leadsto &  \big( \big( e_2 , \gamma_2 \big) ; k_0 \big)\\
  \leadsto &  \big( \big( \RET (e_1) , \gamma_1 \big) ; k_1 \big)\\
  \leadsto &  \big( \big( \VAR (\ell) , {\color{blue}\gamma_4 = \gamma_1[\ell \mapsto ((e_1 , \gamma_1); k_0)] } \big) ; k_0 \big)\\
  \leadsto &  \big( \big( \ell , \gamma_4 \big) ; k_0 \big)\\
  \leadsto &  \big( \big( e_1 , \gamma_1 \big) ; k_0 \big)\\
  \leadsto &  \big( \big( M , \gamma_0 \big) ; k_0 \big)
\end{align*}

%\bibliographystyle{alpha}
%\bibliography{references.bib}

\end{document}
