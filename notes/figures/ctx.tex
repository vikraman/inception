\begin{figure}[!ht]
\fbox{%
\begin{minipage}{\dimexpr\textwidth-2\fboxsep-2\fboxrule\relax}%
\vspace{10pt}%
\centering
\parbox[c]{0.95\linewidth}{%
\centering%

\debugbox{
\begin{subfigure}{0.55\linewidth}
	\setlength{\mathindent}{0pt}
	\begin{align*}
		\text{Types:} && \X \,,\, \Y \,,\, \Z  &:=\; \gls{1} \mid \X \glsop{Rightarrow} \Y \mid \X \glsop{times} \Y \mid \gls{L}\\
		\text{Contexts:} && \GAMMA \,,\, \DELTA &:=\; \gls{varepsilon} \mid \GAMMA \glsbin{bullet} \X\\
		\text{Variables:} && \gls{ell} \,,\, \x \,,\, \y \,,\, \z &:=\; \gls{new} \mid \gls{old} ( \x )
	\end{align*}
  \caption{Grammar}
  \label{fig:ctx-a}
\end{subfigure}
}\hfill\debugbox{
\begin{subfigure}{0.38\linewidth}
  \begin{mathpar}
    \inferrule
      { }
      {\gls{new} \MEMi \X \MEMii \GAMMA , \X}

    \inferrule
      {\x \MEMi \X \MEMii \GAMMA}
      {\gls{old}(\x) \MEMi \X \MEMii \GAMMA \glsbin{bullet} \Y}
  \end{mathpar}
  \vspace{5pt}
  \ifwithagda
  \caption{Typing rules for membership (see \Cref{frm:mem})}
  \else
  \caption{Typing rules for membership}
  \fi
  \label{fig:ctx-b}
\end{subfigure}}

\vspace{20pt}
\debugbox{\begin{subfigure}{0.30\linewidth}
  \begin{ceqn}
    \begin{align*}
      {\LSEM \gls{1} \RSEM}_{\gls{T}} &= \gls{top}\\
      {\LSEM \X \glsop{times} \Y \RSEM}_{\gls{T}} &= {\LSEM \X \RSEM}_{\gls{T}} \glsop{times} {\LSEM \Y \RSEM}_{\gls{T}}\\
      {\LSEM \X \glsop{Rightarrow} \Y \RSEM}_{\gls{T}} &= {\LSEM \X \RSEM}_{\gls{T}} \glsop{Rightarrow} {\gls{T}}{\LSEM \Y \RSEM}_{\gls{T}}\\
      {\LSEM \gls{L} \RSEM}_{\gls{T}} &= \gls{V}
    \end{align*}
  \end{ceqn}
  \ifwithagda
  \caption{Interpretation of types as presheaves in ${\gls{Set}}^{\gls{bF}}$ (see \Cref{frm:semty})}
  \else
  \caption{Interpretation of types as presheaves in ${\gls{Set}}^{\gls{bF}}$}
  \fi
  \label{fig:ctx-c}
\end{subfigure}}
\hfill
\debugbox{\begin{subfigure}{0.25\linewidth}
  \begin{ceqn}
  \begin{align*}
  \\
  {\LSEM \gls{varepsilon} \RSEM}_{\gls{T}} &= \gls{top}\\
  {\LSEM \GAMMA, \X \RSEM}_{\gls{T}} &= {\LSEM \GAMMA \RSEM}_{\gls{T}} \glsop{times} {\LSEM \X \RSEM}_{\gls{T}}\\
  \end{align*}
  \end{ceqn}
  \ifwithagda
  \caption{Interpretation of contexts as presheaves in ${\gls{Set}}^{\gls{bF}}$ (see \Cref{frm:semctx})}
  \else
  \caption{Interpretation of contexts as presheaves in ${\gls{Set}}^{\gls{bF}}$}
  \fi
  \label{fig:ctx-d}
\end{subfigure}}
\hfill
\debugbox{\begin{subfigure}{0.29\linewidth}
  \begin{ceqn}
  \begin{align*}
  {\LSEM \gls{new} \RSEM}_{\gls{T}} &= \PIii\\
  {\LSEM \gls{old}(\x) \RSEM}_{\gls{T}} &= {\LSEM \x \RSEM}_{\gls{T}} \glsop{circ} \PIi\\
  \end{align*}
  \end{ceqn}
  \ifwithagda
  \caption{Interpretation of variables as morphisms ${\LSEM \GAMMA \RSEM}_{\gls{T}} \xrightarrow{{\LSEM \x \RSEM}_{\gls{T}}} {\LSEM \X \RSEM}_{\gls{T}}$ in ${\gls{Set}}^{\gls{bF}}$ (see \Cref{frm:semmem})}
  \else
  \caption{Interpretation of variables as morphisms ${\LSEM \GAMMA \RSEM}_{\gls{T}} \xrightarrow{{\LSEM \x \RSEM}_{\gls{T}}} {\LSEM \X \RSEM}_{\gls{T}}$ in ${\gls{Set}}^{\gls{bF}}$}
  \fi
  \label{fig:ctx-e}
\end{subfigure}}

\vspace{10pt}%
}\end{minipage}}%
\caption{Types, contexts and variables (with \gls{T} a monad on ${\gls{Set}}^{\gls{bF}}$)}
\label{fig:ctx}
\end{figure}
